# Environment variables (NUNCA SUBIR)
.env
.env.local
.env.production

# Node modules
node_modules/

# Build files
dist/
build/

# Python server for local testing
server.py
run-server.bat
__pycache__/
*.pyc

# Chatbot - Solo excluir archivos sensibles
chatbot/worker/.wrangler/
chatbot/worker/.dev.vars
chatbot/worker/node_modules/
chatbot/worker/wrangler.toml

# Admin pages (solo para uso local)
pending.html
js/pending.js

# Documentation interna (no subir a GitHub)
WIKI_PASSWORD_SYSTEM.md